function getNames() {
    var names = ['Bobby', 'Joey', 'Mindy'];
    var ages = [ 21, 18, 20];
    for (var i = 0; i < names.length; i++) {
        document.write(names[i] + " is " + ages[i] + " years old <br />");
    }
}